#include <stdio.h>
#include <stdlib.h>

typedef int ElemType;

typedef struct LNode
{
    ElemType data;
    struct LNode* next;
}LNode,*LinkList;

void Delete(LinkList &L);

int main()
{
    LNode L;head
    LinkList p,head;

    head = &L;
}
